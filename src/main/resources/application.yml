spring:
  jpa:
    hibernate:
      ddl-auto: update

security:
  user:
    password: password

app:
  default:
    user: 'root'
    password: 'password'
    roles:
      admin: 'ADMIN'
      user: 'USER'